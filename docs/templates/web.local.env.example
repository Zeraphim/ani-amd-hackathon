# Copy to web/.env.local before starting `npm run dev` on the Mac.
# The SSH tunnel in docs/07-mi300x-local-runbook.md exposes the remote FastAPI
# service at this local address.
INFERENCE_BASE_URL=http://127.0.0.1:8000
